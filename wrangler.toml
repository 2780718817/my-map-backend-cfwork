# wrangler.toml (已填入ID的最终版)

name = "my-map-backend-cfwork"
main = "src/index.ts"
compatibility_date = "2024-05-01"

# [重要] D1 数据库绑定
[[d1_databases]]
# 注意：我们仍然使用 binding = "DB"，因为我们的代码 (index.ts) 中是通过 env.DB 来访问数据库的，必须保持一致。
binding = "DB"
database_name = "quant"
# ↓↓↓ 我已经将您刚刚生成的真实ID填入其中 ↓↓↓
database_id = "a0349158-e0d8-4878-8eb1-84dfc91e0928"

# [重要] 模块规则 (最现代的写法)
[build]
module_rules = [
  { type = "Text", globs = ["**/*.html"] }
]